rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
  	match /openai/{tokenId} {
      allow read, write;
    }

    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;

      match /projects/{projectId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}